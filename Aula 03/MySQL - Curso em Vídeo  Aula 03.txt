MySQL - Curso em Vídeo  Aula 03

00.
Inside the program XAMPP, you start the services Aopache and MySQL.

01.
DataBase are range of tables and Tables are a range of registers;
Registers are a range of fields (main characteristic of something on a table, like name, age etc);

02. 
To create a new database in MSQLW you digit:
	create database nameoftable;

03. Semicolon indicate the end of a command in database;

04. There are two forms to open a database and run a table (command create table),
you do a double click on a database already created or digite de command: use databasename;

05. To show an table structure, you use the command: describe nameoftable;